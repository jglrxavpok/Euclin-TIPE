func funcReturningFunc(a: Int, b: Real): (Real) -> Real {
    return {
        _: Real // TODO: replace by a inferer-specific instruction
        0.
    }
}

func funcTakingFuncs(f: (Int, Int) -> (Int, Int)): Int {
    return 0
}

func noArgsFunc(): Unit {
    // does nothing
    return ()
}